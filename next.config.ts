import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.pokemon.com", "www.gifcen.com", "media.tenor.com", "media.giphy.com"],
  },
};

export default nextConfig;
